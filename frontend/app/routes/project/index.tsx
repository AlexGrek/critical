import { useOutletContext } from "react-router";

export default function ProjectOverview() {
  // You can access parent loader data via useOutletContext if needed
  return (
    <div>
      <h2>Project Overview</h2>
      {/* Project overview content */}
    </div>
  );
}